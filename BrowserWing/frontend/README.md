# BrowserWing 前端

基于 React + TypeScript + Vite + Tailwind CSS 的现代化前端应用。

## 功能特性

- 🎨 **现代化 UI**: 使用 Tailwind CSS 打造精美界面
- 📱 **响应式设计**: 完美适配各种设备
- ⚡ **快速开发**: Vite 提供极速的开发体验
- 🔒 **类型安全**: TypeScript 确保代码质量

## 快速开始

### 1. 安装依赖

```bash
pnpm install
```

### 2. 配置

复制 `.env.example` 为 `.env`:

```bash
cp .env.example .env
```

### 3. 开发

```bash
pnpm dev
```

应用将在 `http://localhost:5173` 启动。

### 4. 构建

```bash
pnpm build
```

## 项目结构

```
frontend/
├── src/
│   ├── api/          # API 客户端
│   ├── components/   # 可复用组件
│   ├── pages/        # 页面组件
│   ├── App.tsx       # 应用入口
│   ├── main.tsx      # React 入口
│   └── index.css     # 全局样式
├── public/           # 静态资源
└── index.html        # HTML 模板
```

## 页面说明

- **首页 (/)**: 应用介绍和快速入口
- **生成文章 (/generate)**: 配置参数生成新文章
- **素材库 (/articles)**: 查看和管理所有文章
- **文章编辑 (/articles/:id)**: 编辑文章并发布

## 技术栈

- **React 18**: UI 框架
- **TypeScript**: 类型安全
- **Vite**: 构建工具
- **Tailwind CSS**: CSS 框架
- **React Router**: 路由管理
- **Axios**: HTTP 客户端
- **Lucide React**: 图标库

